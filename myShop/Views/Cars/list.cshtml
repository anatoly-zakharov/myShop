@model CarsListViewModel //Эта директива сообщает Visual Studio, что ваше представление ожидает модель, которая является списком объектов Car. После этого IntelliSense должен правильно работать, предоставляя вам подсказки при написании кода C# в List.cshtml.
<h1>Все автомобили</h1>
<h3>@Model.currCategory</h3>
<div class="row mt-5 mt-2">
    @{
        foreach(Car car in Model.getAllCars) {
            @Html.Partial("AllCars",car)
        }
    }
</div>
